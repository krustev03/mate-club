import ProfileForm from './ProfileForm';

const Profile = () => {
  return (
    <section className='centered'>
      <h1>My Profile</h1>
      <ProfileForm />
    </section>
  );
};

export default Profile;