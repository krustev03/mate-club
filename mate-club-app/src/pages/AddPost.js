import PostForm from "../components/posts/PostForm";

const AddPostPage = () => {
  return (
    <section>
      <h1 className="centered">Add Post</h1>
      <PostForm />
    </section>
  );
};

export default AddPostPage;
